// api.js - Módulo externo com dependência
export const fetchUser = async (userId) => {
  // Lógica de busca do usuário na API
  // Por simplicidade, vamos apenas simular uma chamada à API
  return { id: userId, name: "John Doe" };
};
